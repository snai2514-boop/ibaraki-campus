# Android · 教务助手

See the [Japanese / Chinese / English project README](../README.md) for features, downloads, privacy and contact information.

Build using JDK 17 and the Gradle wrapper:

```sh
./gradlew assembleDebug
```

Release signing keys and local configuration are intentionally excluded. See [release documentation](docs/release.md).
