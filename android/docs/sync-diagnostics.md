# 手机同步诊断

同步诊断自动写入应用私有目录 `noBackupFilesDir/sync-diagnostics`，不需要 USB 或电脑持续连接。应用重启后保留；清除应用数据或卸载会删除日志。两个轮转文件合计最多约 512 KiB，旧记录会按容量淘汰。

在「设置 → 导出同步诊断日志」导出文本文件。Android 10 及以上保存到 `Download/CampusAssistant`，稍后可从文件管理分享或连接电脑读取；旧版 Android 使用系统分享。导出在工作线程执行，包含之前进程的记录和本次同步记录。开启反馈中的日志选项时，仅附加最近 15,000 字符的诊断记录。

记录内容：时间、应用版本、随机同步会话号、步骤、耗时、页面可读取状态、是否出现登录控件、导航是否匹配、解析异常类型、网络错误代码和步骤结果。不会记录学号、姓名、密码、Cookie、认证 URL、网页正文或成绩明细。日志不会自动上传。

连接电脑时，可用 `adb logcat -d -s SchoolSync:I` 查看当前系统缓冲区中的同一组事件。跨重启历史以应用内导出的文件为准。

## 2026-09-21 排查

- 旧版在页面不可读取超过 35 秒后把网络中断和登录过期混在一起提示，不能据此确认认证失效。
- 旧版课表入口只匹配可见菜单，资料和成绩入口则允许折叠菜单；已统一课表入口的匹配方式，并增加回归检查。
- 同步状态停止原因同时拼入摘要和详情，导致重复显示；已去重。
- 旧版导出仅抓当前 PID 的 logcat，不能保证断线后和重启前的问题仍有记录；已替换为持久化诊断。
- 实机已确认根因：进入「履修登録・登録状況照会」后，学校先要求确认联系方式，页面有「本人連絡先」和「変更なし」，未直接返回课表。点击「変更なし」后立即进入 2026 年 Q3 课表，未修改联系方式。此前日志持续显示已登录，无登录输入框。
- 新增该确认页的严格识别：立即提示「学校要求确认资料」，提供「查看学校网页」入口；避免反复导航和误报登录过期。日后再次出现时由用户核对学校资料，不自动提交个人资料表单。

## 学校日历与教室

- 实机学校日历使用「节次:课程名@地点」，不提供课程代码。现按本账户、本学年度、完整课程名、日期和节次唯一匹配；含糊的同名课程不写入。
- 支持全角数字、体育馆名称，以及「具体教室,遠隔講義」的地点表示。地点和授课方式分开：已核实线上授课时，日历仍显示线上；不会仅因列出教室就判定面授。
- 学校日历的实际逐日安排独立保存，优先于同日的静态校历计划；跨学年只匹配当年课程。没有公布的日期不沿用 2026 年日期推算；尚未核对完整校历的年度不提供完整学季导出。
- 每次前台访问仍查询学校，假期不再跳过教室查询，未公布不会永久关闭重试。新增数据在之后打开应用同步时读取。当前读取学校日历打开的月份及其边界日期，不声称已经获取尚未展示的全年教室。

## 最终验证（2026-09-21）

- 小米 14 已保留数据覆盖安装，并完成学校同步验证；同页重复导航已抑制。
- 导出的日志包含多次进程重启前的记录，并已从 Download/CampusAssistant 一次性拉取到电脑。未发现姓名、学号或认证 URL。
- Android 单元测试 533 项，0 失败、0 错误、3 跳过；release lint 和构建通过。JS 导航、资料确认页及教室解析回归检查通过。未来学年使用测试样例验证，实际尚未发布的数据仍须待学校发布后实测。

## 2026-09-24 登记开放页面

- 学校开放登记后，在已登记课程下增加独立的「追加登録」行，并把集中授课标题改为「集中講義など | 集中講義を登録」。解析器现在忽略这些已确认的操作标签，继续严格核对星期列数、课程格式和总数。
- 当前学季无可点击的学季链接，解析失败时不再退回模块入口；同一文档、同一目标只导航一次。数据校验失败与网络超时分别报告；单阶段加载失败仍保留记录并继续后续模块。
- 学校当前使用时间格入口和逐门登记按钮。应用提供「学校页面选课」，展示现有学校 WebView，暂停自动导航，并把学校确认框交由用户确认。原生批量提交只适用于已适配的勾选表单；不把当前逐门登记页面误认为批量提交接口。
- 保持第 1—5 限。第 5 限 17:00—18:45；第 6 限暂不增加。没有代用户提交课程。
- 小米 14 实测完整同步成功：Q1 15 门、Q2 14 门、Q3 6 门、Q4 6 门，教室、公告和授课方式阶段均完成。手机已打开学校按时间格查询的候选课程列表。
- 学校候选课程表的手机滚动容器发生高度塌陷，手动选课模式对该容器补充布局样式，保留学校原表单和登记确认流程。
- 最终 1.0.84 构建、release lint 和 535 项单元测试通过（3 项跳过）。覆盖安装后实测横向滑动可看到课程名、远隔授课列和「登録」按钮，未点击登记按钮。

## 1.0.85 原生可登录课程

- 按学校实际提供的时间格入口逐个读取候选课程，直接显示到「可登录课程」原生周历，学校网页移到状态详情里的可选查看入口。
- 小米 14 实测完成 31 个入口，读到 34 门候选课程（含 2 门集中授课）。课程列表、教师与远隔标记、原生勾选切换、第五限候选课程均已核对。测试结束清空选择，没有提交选课。
- 学校当前逐门登记，因此原生页每次选择一门；重新核对账户、学期、时间格、课程完整签名后才可点击学校对应按钮。异常及未知结果保持待核对状态，不自动重复登记。
- 原生候选列表与前台只读履修状态使用独立通知基线，避免普通同步之后再次打开选课页反复报同一批新课程。登记读取过程的阶段和数量写入持久诊断，不写学校网页、账户信息或会话 URL。

## 1.0.86 开课学部

- 从学校候选表的「開講学部」列读取名称，保存到原生课程数据，并在周历课程卡片、候选详情和提交确认页显示；没有提供名称时留空，不按课程代码推测学部。
- 学部名称参与提交前的课程信息核对。小米 14 实测卡片显示「理学部」「地域未来共創学環」「工学部」，原生详情显示「开课学部：理学部」「开课学部：農学部」。未提交选课。
- 首次读取等待页面加载完成，避免在空白文档上误报学校地址异常；菜单、历史和排行重复出现同一入口时，按已确认的学校履修查询流程识别入口。

## 1.0.87 多选课程

- 去掉原生页面勾选新课程时清空其他选择的限制，支持同一时间格和跨时间格多选；提交确认清单列出全部已选课程。
- 学校逐门登记由顺序队列处理：每门提交前重新核对当前课程信息，成功结果持久保存后才处理下一门。失败、取消或结果未知时停止剩余队列；不自动重试或在进程重启后继续提交。
- 原生「查看登记结果」保留已确认与未完成清单。批量队列回归测试覆盖多选、成功推进、停止后保留未完成项、拒绝重复/错序确认以及拒绝已失效的选择。
- 小米 14 实测：同一时间格两个复选框同时选中，再加另一时间格课程，底部显示「已选 3 门」；确认清单显示三门课程和对应学部。随后取消并清空测试选择，没有点击「确认并提交」。真实学校批量登记结果仍须由用户选课后确认。


## 1.0.88 全部发送后统一查询

- 一次确认整份清单，按学校逐门接口依次发送，等待响应返回后继续；中间课表未出现某一门不再打断剩余课程。全部发送后重新打开学校入口，统一查询整批结果并展示登记结果。
- 已批准清单、尝试发送与已发送课程分别持久保存。断网、课程信息变化或未知提交响应时停止发送，转为统一只读查询；再次打开应用只查询待定结果，不补交或重放请求。
- 去掉「我已核对学校结果，重新读取」，改为「重新查询登记结果」。查询成功才解除待定状态；超时或登录过期保留记录，不把已发送当作已登记。
- 未识别的学校额外确认不会被自动接受，也不会逐门弹出确认；取消该提示、停止后续发送后查询本批结果。学校当前普通登记处理器直接提交表单。
- 回归测试覆盖同一时间格与跨格全部发送、统一确认、部分失败继续、未知提交中断、重启仅查询、拒绝重复及错序发送。

- 1.0.88 release 构建与 lint 通过：543 项 Android 测试无失败、3 项跳过；8 项批量状态测试、20 项原生登记适配器检查和 21 项登记页面检查通过。已覆盖安装小米 14，实测读取 30 门候选课程及新流程提示；未提交真实选课。


## 1.0.89 登记结果与工程课程教室

- 最终查询明确区分已登记、未登记和未发送。按课程保存学校可见的拒绝信息与提示框内容，读取并展示学校本学期剩余可登记学分；没有返回额度时不推断为零。不修改或绕过学校上限。
- 课表同步单独提取并保存学校课程名称行，移除教师姓名对课程名匹配的干扰。教室按完整课程名、账户、年度、日期和节次精确匹配；兼容旧快照，刷新后使用结构化名称。线上授课仍不因存在实体教室而改变。
- 自动化测试覆盖工程课程名称、教师后缀、名称截断、日期与节次校验、学校剩余额度、拒绝反馈和最终结果文案。

- 线上课程主卡继续显示授课方式；周历课程详情和月历当日列表补充学校登记教室，并标明不代表面授安排。

- 1.0.89 release 构建与 lint 通过；546 项 Android 测试，0 失败、0 错误、3 项跳过。已在小米 14 覆盖安装，实时同步教室匹配从 18 条增至 28 条；周历实测两门课程显示 D102，线上课程详情补充 D101，主卡保持线上授课。未提交真实选课。
